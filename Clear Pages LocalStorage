javascript:
const localStorageKey = "Sitecore.Pages.LocalXmCloudUrl"; 
const localStorageEntry = localStorage.getItem(localStorageKey); 
if(localStorage){
    localStorage.removeItem(localStorageKey);
    location.reload();
};
